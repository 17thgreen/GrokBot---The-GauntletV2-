# FEAT-20260912-002 — F1-B Structural digital (RV-scaled σ)

- **FEATURE_ID:** FEAT-20260912-002
- **NAME:** Structural P(YES) from moneyness × τ with trailing realized vol
- **STATUS:** INACTIVE
- **FAMILY:** F1
- **WAVE:** FEATURE_WAVE_001_2026-09-12
- **PROVENANCE:** The Statistician · 2026-09-12 UTC · Wave 001 F1 · informal F1-B · sister FEAT-20260912-001 (F1-A frozen σ) · incumbent TEST-20260911-007 · same DATA deps as F1-A · CEM-20260910-001/002/003 different domain
- **CONFIDENCE:** Low [H] — originator prior; measured REDUNDANT/FAIL-INSUFFICIENT on TEST-20260912-002
- **PRETEST_QUERY:** QUERY-20260912-002
- **PRETEST_VERDICT:** CLEAR_TO_HYPOTHESIS
- **TEST:** TEST-20260912-002 — REDUNDANT / FAIL-INSUFFICIENT

## CARD LOCK (2026-09-12 Statistician addendum) [V]
- Scored blend is the single pre-registered λ=0.35 only. Do not fit λ on PM-003.
- Grid {0.20, 0.35, 0.50} = robustness annex; never select by Δ on this slice.
- Headline cells only: `KALSHI|15m|{BTC,ETH}|T-5m|mid`. Other four cells annex-only if measured — no retune after peek.
- No Examiner until L3 Clock-clear.


## DEFINITION
Same K, τ, S_t, Φ digital as F1-A, but σ is estimated at t from L3 1m log returns, not frozen.

Let r_i = ln(c_i/c_{i-1}) on W completed L3 1m bars with bar_end ≤ t.
  σ_t = sqrt( (1/W) ∑ r_i² ) × sqrt(365.25·24·60)   [A: RMS, no mean subtract; 1m→ann]
  z_t = ln(S_t / K) / (σ_t √τ)
  p_struct = Φ(z_t)
  p_t = clip( (1-λ) m_t + λ p_struct, ε, 1-ε )

If W incomplete or σ_t = 0 → missing (no substitute frozen σ — that would collapse into F1-A).

### PRE-REGISTERED PRIMARY CELL (no post-hoc switch)
Same headlines: `KALSHI|15m|BTC|T-5m|mid` and `KALSHI|15m|ETH|T-5m|mid`.
Primary: W=60, λ=0.35, ε=1e-4.
Robustness: W∈{30,60,120}; λ∈{0.20,0.35,0.50}; all six cells. Do not pick best W after seeing Δ. Do not pick λ from PM-003 Δ.

## MECHANISM
Conditional remaining variance should scale the digital: high realized vol → z shrinks → p_struct → 0.5; low vol → z inflates → more extreme structural P. Incremental vs F1-A is that time-varying σ beats a frozen σ in blend with m_t. [H]
If F1-B does not beat F1-A on Δ vs mid, RV term is REDUNDANT (not a third card).

## CONTRACT / UNIVERSE SCOPE
Identical to F1-A (PM-003 1208; six cells; no pool).

## DECISION-TIME / KNOWABILITY
Same as F1-A plus: σ_t uses only completed 1m bars ≤ t. No intra-bar. No EXPIRATION_VALUE. Same L3 DATA-* gate.

## DATA LAYER
Same L1 + required L3 1m path as F1-A (need enough pre-window 1m history for W=120 warmup). L2 CF replay still unused.

## TRANSFORMS / NORMALIZATION
No mean-subtract on r_i. No winsor on r_i in primary (robustness: 1% two-sided winsor of r_i — report only). clip only on p_t.

## EXPECTED SIGN / USE IN FORECAST
Same blend role as F1-A. Higher σ_t pulls p_struct toward 1/2. Atomic vs F1-A — do not average F1-A and F1-B this wave (no score-of-scores). λ scored = 0.35 (CARD LOCK).

## FAILURE REGIME
RV jumps after t; 1m RV noisy at W=30; L3 ≠ CF; mid already embeds RV; weekend.

## FALSIFICATION
On each headline cell, incremental model vs incumbent mid (same N, same t):
Kill / REDUNDANT if ΔBrier ≥ 0 and ΔLogLoss ≥ 0 (model − market; lower-is-better) or CIs cover 0 after pre-registered method.
Also kill if only one asset “works” and the other inverts (no pooled rescue).
Plus: if F1-B Δ is no better than F1-A Δ on the same cells / same N → RV term REDUNDANT (do not open a third card).
Placebos: (1) shuffle σ_t across rows leaving (S_t,K,τ) fixed; (2) permute r-path timestamps; (3) λ=0 (must give Δ=0).
Do not score EXPIRATION_VALUE.


## LEAKAGE & REDUNDANCY CONTROLS
- Ablation vs m_t alone (λ=0) required.
- Ablation vs F1-A (frozen σ) required — F1-B must earn keep vs sister.
- Ablation vs τ-only / no S_t cousin required.
- Cross-layer: not F2; not F3.
- Slice USED_RESEARCH — RESEARCH only, not validation/sealed.

## Evidence notes
Thesis [H]. RV estimator / W / λ / ε [A]. Mid+τ [V] on PM-003 CLEARED. S_t / σ_t L3 [U] until DATA-*. Measurement: TEST-20260912-002 REDUNDANT/FAIL-INSUFFICIENT. Trade FORBIDDEN.

## Archivist filing note (2026-09-12)
- Canonical ID lock [V]: F1 = FEAT-20260912-001/002; F3 = 003/004.
- Intake FALSIFICATION was truncated once; Archivist completed from stated intent — Statistician may still patch.
- STATUS HYPOTHESIS. No TEST. L3 DATA gate open.

## Statistician CONFIRM+PATCH (2026-09-12T00:13:05Z)
Exact FALSIFICATION block replaced per Statistician. Placebos expanded. λ/CELL LOCK already on card. **Canonical ID = FEAT-20260912-002** (not 004 — 004 is F3 last−mid).

## Examiner result — TEST-20260912-002 (2026-09-12T00:20:53Z)
- **VERDICT:** REDUNDANT / FAIL-INSUFFICIENT (not NO_EDGE)
- Headlines λ=0.35: BTC T-5m ΔBrier=+0.001072 ΔLogLoss=+0.002517 (N=569); ETH T-5m ΔBrier=+0.001216 ΔLogLoss=+0.003705 (N=545) — both ≥0 vs mid
- **RV vs sister:** 002 strictly better Δ than 001 on both headlines (earns keep vs F1-A) but still fails vs market — **no third card**
- Annex: BTC T-10m alone INCREMENTAL_RESEARCH (not headline rescue)
- Placebo shuffle σ_t fails; λ=0 Δ≈0
- Artifacts: `archive/tests/TEST-20260912-002-F1-INCREMENTAL.{md,json}`
- **No cemetery**
- Trade FORBIDDEN; invented_numbers=false
