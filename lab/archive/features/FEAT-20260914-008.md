# FEAT-20260914-008 — F4 wick-gated structural Φ(z)

- **FEATURE_ID:** FEAT-20260914-008
- **NAME:** Wick-gated structural Φ(z) blend vs Kalshi mid
- **STATUS:** INACTIVE
- **TEST:** TEST-20260914-001 — REDUNDANT / FAIL-INSUFFICIENT
- **DRAFT_ID:** DRAFT-FEAT-20260914-008
- **CARD_KIND:** FEATURE (one instrument)
- **WAVE:** F4 / Wave 009 (`governance/FEATURE_ORDER_F4_2026-09-14.md` · `governance/WAVE_009_ORTHOGONAL_SLOT_2026-09-14.md`)
- **PROVENANCE:** The Statistician · 2026-09-14 UTC · compose DRAFT-FEAT-20260912-006 onto DRAFT-ABST-20260914-008-F4-wick · Governor SIGN F4 · tape Sep-12 PM-004 × L3-002 lock B + L3-001 edges · not FEAT-002 / TEST-20260912-002 (unconditional F1) · not FEAT-20260913-007 (W2-B) · not FEAT-20260913-005 (CB-VEL) · 006 left on disk unedited
- **CONFIDENCE:** Low [H] — same digital that lost unconditionally; increment is the wick gate only
- **TRADE:** FORBIDDEN
- **EXAMINER:** Not authorized by this filing. Statistician does not lift Examiner.
- **LEARNED_CLASS:** none (no fit; θ/W/λ frozen on 006)

## INSTRUMENT

**Wick-gated structural digital.** Speak only on wick rows. Off-wick and ABSTAIN → \(p_t = m_t\).

Structural map is the frozen F1-B digital from `DRAFT-FEAT-20260912-006` (not retuned):

- \(S_t\) = last L3 1m close under **lock B** (`bar_end < decision_time`)
- \(K\) = FLOOR_STRIKE (PM-001), knowable after OPEN
- \(\hat\sigma_t\) = RMS of prior \(W_\sigma=60\) completed 1m log returns, annualized \(\times\sqrt{365.25\cdot24\cdot60}\)
- \(z_t = \ln(S_t/K)/(\hat\sigma_t\sqrt{\tau})\)
- \(p_{\mathrm{struct}} = \Phi(z_t)\)

Wick (frozen on 006; not searched):

\[
\text{wick} := \bigl|\ln(S_t/S_{t-W})\bigr| > \theta\cdot\hat\sigma_t\cdot\sqrt{W/(365.25\cdot24\cdot60)}
\]

with **θ = 1.5**, **W = 5**. Not a cheap-binary wick. Not \(|m_t - p_{\mathrm{struct}}|\).

L3 = external predictor. **≠** CF settlement oracle.

## GATE COMPOSITION

Compose with `DRAFT-ABST-20260914-008-F4-wick.md`. Do **not** retune it. Do **not** edit `DRAFT-FEAT-20260912-006`.

- Cartographer ABSTAIN / missing lookback → \(p_t := m_t\)
- ALLOW + off-wick → Feature silence \(p_t := m_t\) (not a second headline)
- Speak (blend) only on ALLOW + wick
- **No annex speak.** T-10m report-only if ever scored. T-14m forbidden (lookback crosses open).

**Headlines (no pool):**
1. `KALSHI|15m|BTC|T-5m|mid`
2. `KALSHI|15m|ETH|T-5m|mid`

## DEFINITION

```
ε   = 1e-4
θ   = 1.5            # frozen on 006 [A] — do not search
W   = 5              # completed 1m bars; frozen [A]
λ   = 0.35           # Wave 001 lock; frozen [A]
Wσ  = 60             # RV window; same as FEAT-002 / 006; drop if incomplete
# robustness annex only (never select by Δ): λ ∈ {0.20, 0.35, 0.50}
# λ = 0 diagnostic ⇒ Δ = 0
# no θ/W grid — those are locked

m_t    = PM-004 same-row mid at decision_time     # Sep-12 only; method=mid
bar    = last L3-002 (or Clock-named L3-001 edge) 1m with bar_end < t   # LOCK B
S_t    = bar.close
S_tW   = close of the bar W completed steps back in that same lock-B chain
σhat   = RMS(log returns of prior Wσ completed lock-B bars) * sqrt(365.25*24*60)
τ      = time_remaining_sec / (365.25*24*3600)
K      = FLOOR_STRIKE
z      = ln(S_t / K) / (σhat * sqrt(τ))
p_s    = Φ(z)

lookback_ok = (S_t, S_tW present) and (σhat defined) and (K known) and (τ > 0)
wick        = abs(ln(S_t / S_tW)) > θ * σhat * sqrt(W / (365.25*24*60))

if gate ABSTAIN or m_t missing or not lookback_ok:
    p_t = m_t
elif not wick:
    p_t = m_t          # Feature silence; Δ = 0 on these rows by construction
else:
    p_t = clip((1-λ)*m_t + λ*p_s, ε, 1-ε)
```

AMD-001: scored \(p_t \in (\varepsilon,1-\varepsilon)\). No raw 0/1. Same-t PM-004 mid only.

## MECHANISM

Unconditional F1 (FEAT-002 / TEST-20260912-002) spoke on all rows and died [V]. A large short-horizon L3 move may be partly transitory; if the binary mid overshoots, \(m_t\) can sit farther from P(resolution) than \(p_{\mathrm{struct}}\) **on those rows only**. [H]

F4 is a selection gate, not an F1 λ/σ retune. Off-wick the card is silent. Not algebraic lock (Map 1). Not F2 close-minute. Not W2-B.

## CONTRACT / UNIVERSE SCOPE

- Tape: **Sep-12** DATA-PROV-PM-004 mid × L3-002 lock B + L3-001 Sep-11 midnight edges (Clock-named). Coverage named 855/855 [V order]
- **No PM-003.** No Sep-13. No union with PM-002
- VENUE_ADAPTER: KALSHI. No Poly. No BTC+ETH pool
- L3 ≠ oracle; no CF/Binance fill of missing L3 closes

## DECISION-TIME / KNOWABILITY

- \(m_t\), rem=300: PM-004 same row [V]
- \(K\): PM-001 FLOOR_STRIKE after OPEN [V]
- \(S_t, S_{t-W}, \hat\sigma_t\): L3 lock **B** only (`bar_end < t`) [V gate]
- Missing W=5 chain or incomplete \(W_\sigma\) → ABSTAIN, not “no wick”
- FORBIDDEN: PM-003 peek; policy A (`bar_end ≤ t`) as scored join; CF-as-oracle; EXPIRATION_VALUE; last-as-mid; Map 2; T-14m; W2-B last; signed CB-VEL

## DATA LAYER

- L1: DATA-PROV-PM-004 Sep-12 T-5m mid; DATA-PROV-PM-001 FLOOR_STRIKE / CLOSE_TIME
- L2 oracle: not used
- L3: DATA-PROV-L3-002 lock B + DATA-PROV-L3-001 edges as Clock-named (not L3-001 as the scored tape)
- Intra-window 50¢→15¢ path: not used

## TRANSFORMS / NORMALIZATION

- Missing mid / lookback / σ̂ / K → ABSTAIN, no fill
- No winsor of \(\ln(S/K)\) or \(\ln(S_t/S_{t-W})\)
- clip \(p_t\) to \((\varepsilon,1-\varepsilon)\) only
- No learned weights. No θ/W/λ fit

## EXPECTED SIGN / USE IN FORECAST

On wick rows, blend pulls \(m_t\) toward \(p_{\mathrm{struct}}\). Off wick, \(p_t = m_t\). Not an ensemble. Not a trade.

## FAILURE REGIME

- Wick is information, not overreaction (mid already right → blend hurts)
- θ too high → N_wick < 80 → FAIL-INSUFFICIENT
- θ too low → F1 redux (always-speak)
- L3 ≠ CF TWAP basis
- T-5m near-deg cluster
- Incomplete lock-B chain → chronic ABSTAIN

## FALSIFICATION

On **each** headline separately vs same-t PM-004 mid, **full cell** (not a wick-only leaderboard):

Skill requires **both** ΔBrier < 0 and ΔLogLoss < 0 (model − market).  
Kill / REDUNDANT if ΔBrier ≥ 0 **or** ΔLogLoss ≥ 0, or CIs cover 0.  
Kill / FAIL-INSUFFICIENT if **N_wick < 80** on that headline (do not lower after peek).  
Kill if one asset works and the other inverts (no pool).

Placebos:
1. Shuffle wick labels within cell
2. Reverse gate (blend only when **no** wick)
3. Sign-flip \(p_{\mathrm{struct}}\) on wick rows
4. λ = 0 (must give Δ = 0)
5. Unconditional F1 (always blend) — must not be this card

Report N_wick and wick-subset Δ as annex only. Do not retune θ/W/λ after the sheet. Do not open T-14m. Do not peek PM-003. Do not lift Examiner from this filing.

## LEAKAGE & REDUNDANCY CONTROLS

- Ablation vs \(m_t\) alone is binding
- Ablation vs always-blend F1 (FEAT-002 map) is diagnostic, not a rescue
- Not W2-B / CB-VEL / Map 2 / cheap-binary wick / \(|m-p_{\mathrm{struct}}|\) wick
- USED_RESEARCH if scored on Sep-12 PM-004
- Holdout closed

## Evidence notes

Thesis [H]. θ=1.5 / W=5 / λ=0.35 / ε / Wσ=60 [A] frozen on 006 before this compose. Headlines + lock B + Sep-12 PM-004 [V]. L3 ≠ oracle [V]. 006 not edited [V]. Measurement **UNTESTED**. No invented Δ. Trade FORBIDDEN. Examiner not lifted.

## Examiner result — TEST-20260914-001 (2026-09-14T01:05:59Z)
- **VERDICT:** REDUNDANT / FAIL-INSUFFICIENT (not NO_EDGE)
- Both headlines N_wick < 80: BTC N_wick=12; ETH N_wick=10
- BTC full-cell: ΔBrier=-0.000107 ΔLogLoss=+0.003648 N=96 — LogLoss≥0 + N_wick kill
- ETH full-cell: ΔBrier=-0.000238 ΔLogLoss=-0.001766 N=95 — both Δ<0 but N_wick kill
- θ=1.5 W=5 λ=0.35 frozen — **no θ/W/λ retune**
- Sep-12 PM-004 only; lock B; L3 ≠ oracle; no PM-003
- Gate: DRAFT-ABST-20260914-008
- Artifacts: `archive/tests/TEST-20260914-001-F4-WICK-INCREMENTAL.{md,json}`
- **No cemetery**
- Trade FORBIDDEN; invented_numbers=false
