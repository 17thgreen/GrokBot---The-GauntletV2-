# FEAT-20260913-001 — W2-C sibling-window mid

- **FEATURE_ID:** FEAT-20260913-001
- **NAME:** Same-window sibling-asset mid blend
- **STATUS:** INACTIVE
- **TEST:** TEST-20260913-002 — REDUNDANT / FAIL-INSUFFICIENT
- **DRAFT_ID:** DRAFT-FEAT-20260913-001
- **CARD_KIND:** FEATURE (one instrument)
- **WAVE:** W2-C (`governance/FEATURE_ORDER_W2C_2026-09-13.md`)
- **PROVENANCE:** The Statistician · 2026-09-13 UTC · compose DRAFT-ABST-20260913-001 · incumbent MKT-KALSHI-15M-MID / TEST-20260911-007 · DATA-PROV-PM-003 · AMD-20260913-001/005 · not FEAT-20260912-001/002/003/004/005 · not DRAFT-FEAT-20260912-006 · EDGE-20260910-004 / CEM-20260910-003 cited as different domain (OHLCV lead/lag, not resolution \(p_t\))
- **CONFIDENCE:** Low [H] — originator prior; measured REDUNDANT/FAIL-INSUFFICIENT on TEST-20260913-002
- **TRADE:** FORBIDDEN
- **LEARNED_CLASS:** none (packet §1.4 not invoked; no fit on PM-003)

## INSTRUMENT (why this is not a renamed dead Feature)

New L1 instrument: the **other asset’s same-window, same-\(t\) mid** \(m^\star_t\).

Not F1: no \(S_t\), \(K\), \(\sigma\), \(\Phi(z)\), moneyness.  
Not F3: no spread, no last−mid.  
Not F2: no CF remainder, no \(k\), no T−1 incumbent.  
Not F4 wick-gated digital.  
Not a new adjective on FEAT-001…005.

## GATE COMPOSITION (binding)

Compose with `DRAFT-ABST-20260913-001-W2C-bad-cal.md`. Do **not** retune that gate.

- On ABSTAIN: \(p_t := m_t\) (Δ = 0 vs incumbent by construction).
- Speak only on ALLOW_SPEAK_HEADLINE (and ALLOW_SPEAK_ANNEX only if this card opts in — **it does not**).

**Annex `KALSHI|15m|BTC|T-14m|mid`:** **DARK.** This card does **not** opt in. Silent would also be dark; this line makes it explicit.

**Headlines (AMD-005, no pool):**
1. `KALSHI|15m|ETH|T-14m|mid`
2. `KALSHI|15m|BTC|T-10m|mid`

## DEFINITION

Let \(m_t\) = TEST-007 same-row mid on the scored contract (same-\(t\); `implied_p_method == mid`).

**Sibling row** \(m^\star_t\):
- Same `VENUE_ADAPTER` = KALSHI, same 15m window (`OPEN_TIME`, `CLOSE_TIME` from DATA-PROV-PM-001), opposite `asset` (BTC↔ETH)
- Same checkpoint `time_remaining_sec` ∈ {840, 600} matching the scored cell’s T-minus map [V]
- `implied_p_method == mid` on the sibling row
- Same TEST-007 excludes on the sibling row: drop last-fallback; drop if sibling `implied_p` ≤ 0.02 or ≥ 0.98; VOID/DISPUTED sibling → missing

**Frozen map (no fit):**
```
ε = 1e-4
λ = 0.25          # single scored blend; not fit on PM-003
# robustness annex only (never select by Δ): λ ∈ {0.15, 0.25, 0.35}
# λ = 0 diagnostic must give Δ = 0

if DRAFT-ABST-20260913-001 says ABSTAIN:
    p_t = m_t
elif sibling missing / Clock cannot join:
    p_t = m_t      # fail-closed; no speak
else:
    p_t = clip( (1-λ) * m_t + λ * m_star_t, ε, 1-ε )
```

AMD-001: scored \(p_t \in (\varepsilon,1-\varepsilon)\). No raw 0/1. ε-clip is hygiene, not uncertainty. No algebraic lock claimed.

## MECHANISM

ETH and BTC 15m Up/Down share a crypto factor at the same window. On cells where TEST-007 already measured mid ECE above the locked median, own-mid may be noisier / slower than the contemporaneous sibling mid. A frozen convex blend pulls \(p_t\) toward that other contract’s implied P without L2 oracle or L3 spot. [H]

This is **not** EDGE-004 (BTC return → ETH forward return). Target here is contract resolution vs same-\(t\) mid. [V] different domain; cite CEM-20260910-003.

## CONTRACT / UNIVERSE SCOPE

- Universe: DATA-PROV-PM-003 1208, TEST-007 excludes (mid-only, no last-fallback, drop \(m_t \le 0.02\) or \(\ge 0.98\), VOID/DISPUTED out).
- VENUE_ADAPTER: KALSHI only. No Poly. No BTC+ETH score pool. No checkpoint pool.
- PM-002: do not union. Holdout closed. USED_RESEARCH if measured.

## DECISION-TIME / KNOWABILITY

- \(m_t\), rem, asset: PM-003 checkpoint row — knowable [V] on CLEARED mid cells.
- \(m^\star_t\): sibling checkpoint at the **same** `decision_time` / rem — knowable if that row exists [A] pairing; Clock must certify join without look-ahead (no future rem, no later-revised mid).
- Gate allowlist: frozen constants, not estimated at \(t\) [V].
- FORBIDDEN at \(t\): EXPIRATION_VALUE, T−1 mid as incumbent, last-fallback-as-mid, CF/Binance as oracle, OI/funding, L2 book.

## DATA LAYER

- L1 PM: DATA-PROV-PM-003 (both assets’ checkpoint mids); DATA-PROV-PM-001 (OPEN/CLOSE / asset pairing).
- L2 / L3: **not used**.
- Fields: `contract_id`, `asset`, `time_remaining_sec`, `implied_p`, `implied_p_method`, `decision_time`, OPEN/CLOSE for sibling key.

## TRANSFORMS / NORMALIZATION

- No silent drops except declared missing sibling / gate ABSTAIN / TEST-007 excludes.
- clip only for AMD-001 hygiene.
- No winsor of \(m^\star_t\).
- No learned weights.

## EXPECTED SIGN / USE IN FORECAST

- \(p_t\) moves from \(m_t\) toward \(m^\star_t\) by fraction λ=0.25 on speak rows only.
- Not an ensemble. Not averaged with F1/F3. Not a trade.

## FAILURE REGIME

Asset-idiosyncratic news; missing sibling (fail-closed → mid); sibling mid already redundant with own mid; λ too large; pairing mismatch (Clock DEV_FAIL on join); annex sneak / pool.

## FALSIFICATION

On **each** headline separately (same N, same \(t\), same-\(t\) mid):

Skill requires **both** ΔBrier < 0 and ΔLogLoss < 0 (model − market; lower-is-better). Examiner kill is both-Δ: REDUNDANT if ΔBrier ≥ 0 **or** ΔLogLoss ≥ 0, or if CIs cover 0 after Examiner’s pre-registered method. The “and” wording is void — it does not soften the door.

Also kill if one headline “works” and the other inverts (no pooled rescue).

Placebos (required):
1. Shuffle \(m^\star_t\) within cell, leave \(m_t\) fixed
2. λ = 0 (must give Δ = 0)
3. Random opposite-window sibling (wrong OPEN/CLOSE) should not beat the true pairing

Do not score raw 0/1. Do not retune λ or the W2-C gate after the sheet.

## LEAKAGE & REDUNDANCY CONTROLS

- Ablation vs \(m_t\) alone (λ=0 / ABSTAIN-all) required.
- Ablation vs τ-only / asset-only (no sibling) — if those match this card, instrument is not sibling-mid.
- Not F1/F2/F3/F4. Not W2-B oracle divergence.
- Slice USED_RESEARCH if run on PM-003 — RESEARCH only, not validation, not sealed.

## Evidence notes

Thesis/mechanism [H]. ECE table / gate / headlines [V] TEST-007 + slot + DRAFT-ABST. λ/ε [A]. Sibling pairing completeness [U] until Clock. Measurement **UNTESTED**. No invented Δ, fills, or Sharpes. Trade FORBIDDEN.

## Examiner result — TEST-20260913-002 (2026-09-13T19:17:54Z)
- **STATUS:** INACTIVE
- **VERDICT:** REDUNDANT / FAIL-INSUFFICIENT (not NO_EDGE; not Champion)
- Package kill: one headline incremental (`KALSHI|15m|BTC|T-10m|mid` ΔBrier=-0.000645 ΔLogLoss=-0.001880 N=604), other fails/inverts (`KALSHI|15m|ETH|T-14m|mid` ΔBrier=+0.000480 ΔLogLoss=+0.000947 N=604) — **no pooled rescue**
- Gate: DRAFT-ABST-20260913-001 compose; annex BTC T-14m DARK
- Frozen λ=0.25; robustness λ∈{0.15,0.25,0.35} — do not pick winner
- Artifacts: `archive/tests/TEST-20260913-002-W2C-INCREMENTAL.{md,json}`
- **No cemetery** — REDUNDANT/FAIL-INSUFFICIENT ≠ NO_EDGE
- DATA: PM-003 USED_RESEARCH; sibling join CLEARED; no Poly/CF/L3/EXPIRATION_VALUE
- Trade FORBIDDEN; invented_numbers=false
