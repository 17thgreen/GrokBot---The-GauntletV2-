# DRAFT-FEAT-20260912-006 — F4 wick-gated structural digital
**STATUS:** DRAFT / NOT COMMISSIONED — HYPOTHESIS if frozen  
**FEATURE_ID:** FEAT-20260912-006 (provisional stamp; Archivist confirms on commission)  
**NAME:** Wick-gated structural P(YES) vs Kalshi mid  
**FAMILY:** F4 (new). Not F1-C. F1-001/002 stay INACTIVE.  
**WAVE:** none until Governor signs `DRAFT_GOV_ORDER_F4_WICK_2026-09-12.md`  
**PROVENANCE:** Logan narrative 2026-09-12 (mid-contract overreaction / minutes 5–10) · Conductor filing shape · cites TEST-20260912-002 F1 kill  
**CONFIDENCE:** Low [H] — same digital that lost unconditionally; increment is the gate only

## DEFINITION
Kalshi 15m YES iff end-60s CF RTI simple average ≥ FLOOR_STRIKE (start-60s CF average) `[V]`.  
Settlement is **not** a 15-minute rolling average. Mid-window prints are **not** in the settlement TWAP `[V]`.

Reuse F1-B structural map, frozen, no-fit `[A]`:
- \(S_t\) = last fully completed L3 1m close with `close_time_ms ≤ decision_time_ms` (DATA-PROV-L3-001 join; **not** oracle)
- \(K\) = FLOOR_STRIKE (PM-001)
- \(\tau\) = time_remaining_sec / (365.25·24·3600)
- \(\hat\sigma_t\) = RMS of prior W_σ=60 completed 1m log returns, annualized ×√(365.25·24·60) — same as FEAT-002; drop row if W_σ incomplete (no frozen-σ fallback)
- \(z_t = \ln(S_t/K)/(\hat\sigma_t\sqrt{\tau})\)
- \(p_{\mathrm{struct}} = \Phi(z_t)\)

**Wick (the new cut — lock before looking):**
- W = 5 completed 1m bars ending at the knowable bar for \(S_t\)
- \(S_{t-W}\) = close of that fifth-back completed bar (`close_time_ms ≤ decision_time_ms`)
- wick := \(|\ln(S_t/S_{t-W})| > \theta \cdot \hat\sigma_t \cdot \sqrt{W / (365.25\cdot24\cdot60)}\)
- **θ = 1.5** frozen. Not fit. Not a bps search.
- If \(S_{t-W}\) missing → feature missing (drop), not “no wick”

**Forecast map (frozen):**
- no wick: \(p_t = m_t\)  (identical to incumbent; those rows contribute Δ = 0 by construction)
- wick: \(p_t = \mathrm{clip}((1-\lambda)m_t + \lambda p_{\mathrm{struct}}, 10^{-4}, 1-10^{-4})\)
- **λ = 0.35** (same Wave 001 lock). Grid {0.20, 0.35, 0.50} = annex only. Do not pick winner.

Wick is a **spot** event `[A]`. It is **not** “binary printed 15¢” and **not** \(|m_t - p_{\mathrm{struct}}| > 0.15\).

## MECHANISM
Claim `[H]`: a large short-horizon spot move is partly transitory; retail binaries overshoot; \(m_t\) then lies farther from P(resolution) than \(p_{\mathrm{struct}}\). Unconditional F1 already lost `[V]`; this card says the residual lives **only** on wick rows.

Not claimed: algebraic impossibility of hitting K with 5–10 minutes left (false for BTC at typical gaps). Not claimed: last-10s lock (F2 / FEAT-005).

## CONTRACT / UNIVERSE SCOPE
- Preferred: new Kalshi 15m BTC/ETH with OPEN after `2026-09-11T20:15Z` (post PM-003). New DATA-* + Clock before Examiner.
- If Governor allows a contaminated RESEARCH peek: DATA-PROV-PM-003 1208 only, same TEST-007 excludes (mid-only, drop last-fallback, drop implied_p ≤ 0.02 or ≥ 0.98, VOID out). **Not** validation.
- VENUE_ADAPTER: KALSHI. No Poly. No BTC+ETH pool. No checkpoint pool.
- PM-002: do not union.

## DECISION-TIME / KNOWABILITY
- \(m_t, \tau, K\): same as F1 / TEST-007 `[V]`
- \(S_t, S_{t-W}, \hat\sigma_t\): completed-bar join only `[V]` L3 freeze. No incomplete bar.
- FORBIDDEN at t: EXPIRATION_VALUE, terminal last, CF-as-oracle, last-fallback-as-mid
- Latency: each lookback bar usable only after its close_time_ms

## DATA LAYER
- L1: PM-003 or successor checkpoints + PM-001 FLOOR_STRIKE / RESOLUTION
- L2 oracle: not used (F2 owns it)
- L3: DATA-PROV-L3-001 (old slice) or a new L3 covering post-2026-09-11
- Intra-window binary path 50¢→15¢: **not required** and **not used**. PM-003 does not have a continuous mid tape `[V]`.

## TRANSFORMS / NORMALIZATION
- No silent drops except declared missing S / incomplete RV / exclude rules
- No winsor of ln(S/K) or ln(S_t/S_{t-W})
- clip p only for logloss

## EXPECTED SIGN / USE IN FORECAST
On wick rows, blend pulls \(m_t\) toward \(p_{\mathrm{struct}}\). Off wick, abstain (p = m). Not a trade. Not an ensemble with F1/F3.

## FAILURE REGIME
Wick is information, not overreaction (mid already right → blend hurts). θ too high → N_wick < 80. θ too low → F1 redux. L3 ≠ CF TWAP basis. T-5m near-deg cluster.

## FALSIFICATION (pre-registered)
Headline cells (separate): `KALSHI|15m|BTC|T-5m|mid` and `KALSHI|15m|ETH|T-5m|mid`.  
Annex only: T-10m. **Not** T-14m (lookback crosses open).

On each headline, same N as incumbent mid after excludes:
- Kill / REDUNDANT if ΔBrier ≥ 0 **or** ΔLogLoss ≥ 0 (model − market)
- Kill / FAIL-INSUFFICIENT if N_wick < **80** on that cell (do not lower after peek)
- Kill if one asset works and the other inverts (no pool)
- Placebos (must not be the thing that “works”): shuffle wick labels within cell; reverse gate (blend only when **no** wick); sign-flip \(p_{\mathrm{struct}}\) on wick rows
- λ=0 must give Δ = 0

Skill on the **full** cell, not a post-hoc wick-only leaderboard. Report N_wick and wick-subset Δ as annex.

## LEAKAGE & REDUNDANCY CONTROLS
- Ablation vs m_t alone is the test
- Ablation vs unconditional F1 (FEAT-002 map on all rows) — F4 must beat *both* mid and “always blend” if we score the old slice; on a new slice, vs mid is binding
- No MLE. No 15¢ BUY threshold. EV UNTESTED. Trading FORBIDDEN
- TEST-002 annex must not choose θ, W, λ, or headline

## Evidence notes
- F1 unconditional kill: `[V]` TEST-20260912-002
- Settlement = last 60s CF vs floor: `[V]` Kalshi rules / PM-001
- Wick overreaction: `[H]`
- θ=1.5, W=5, λ=0.35, N_wick≥80: `[A]` frozen
- Retail 50¢→15¢ frequency on this venue: `[U]` / not used
- Promotion on PM-003: **forbidden** `[V]` contamination

## Lineage note (2026-09-14T01:05:59Z)
Measured as `FEAT-20260914-008` / TEST-20260914-001 — REDUNDANT / FAIL-INSUFFICIENT. This Sep-12 draft is lineage only; do not retune.
