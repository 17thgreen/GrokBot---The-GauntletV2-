# FEAT-20260913-005 — CB-VEL Coinbase 1m return clip

- **FEATURE_ID:** FEAT-20260913-005
- **NAME:** Frozen linear clip of Coinbase 1m completed-bar return into Kalshi mid
- **STATUS:** INACTIVE
- **TEST:** TEST-20260913-005 — REDUNDANT / FAIL-INSUFFICIENT
- **DRAFT_ID:** DRAFT-FEAT-20260913-005
- **CARD_KIND:** FEATURE (one instrument)
- **WAVE:** CB-VEL / Wave 007 (`governance/FEATURE_ORDER_CBVEL_2026-09-13.md`)
- **PROVENANCE:** The Statistician · 2026-09-13 UTC · compose DRAFT-ABST-20260913-005-CBVEL · lock B (`CBVEL_ON_MINUTE_POLICY`) · DATA-PROV-CB-001 · incumbent TEST-20260911-007 same-t mid · not FEAT-20260913-003 (CF−mid) · not FEAT-20260913-002 (L3-strike) · not FEAT-20260913-004 (Poly last) · not FEAT-005 Map 2
- **CONFIDENCE:** Low [H] — originator prior; measured REDUNDANT/FAIL-INSUFFICIENT on TEST-20260913-005
- **TRADE:** FORBIDDEN
- **LEARNED_CLASS:** none (no fit on PM-003)

## INSTRUMENT

Coinbase 1m **completed-bar simple return** under lock **B** only:

`v_CB,t = (close_B − close_{B-1}) / close_{B-1}`

where B is the last DATA-PROV-CB-001 1m candle with `bar_end < decision_time`, and `close_{B-1}` is the prior completed Coinbase 1m close. [V policy B]

Frozen linear clip into same-t Kalshi mid. Not CF. Not strike. Not Poly last. Not Map 2. Not Binance. Not policy A (`bar_end <= t`).

## GATE COMPOSITION

Compose with `DRAFT-ABST-20260913-005-CBVEL.md`. Do **not** retune it.

- ABSTAIN → \(p_t := m_t\)
- Speak only on ALLOW_SPEAK_HEADLINE
- **No annex.** Policy A is not a headline and not a rescue.

**Headlines (no pool):**
1. `KALSHI|15m|BTC|T-14m|mid`
2. `KALSHI|15m|ETH|T-14m|mid`

## DEFINITION

```
ε = 1e-4
w = 0.0015         # 15 bps 1m return saturates clip; frozen [A]
λ = 0.15           # single scored weight; frozen [A]
# robustness annex only (never select by Δ): λ ∈ {0.10, 0.15, 0.20}; w ∈ {0.0010, 0.0015, 0.0025}
# λ = 0 diagnostic ⇒ Δ = 0

bar   = last CB-001 1m candle with bar_end < decision_time     # LOCK B
prior = immediately previous completed CB-001 1m close
v     = (bar.close - prior.close) / prior.close

if gate ABSTAIN or bar missing or prior missing or prior.close <= 0:
    p_t = m_t
else:
    p_t = clip(m_t + λ * clip(v / w, -1, +1), ε, 1-ε)
```

AMD-001: scored \(p_t \in (\varepsilon,1-\varepsilon)\). No raw 0/1. Same-t mid only.

## MECHANISM

A just-completed Coinbase minute is a BRTI *constituent* print, not the settlement TWAP. At T-14m (uncertainty, not close-minute lock) a short exchange return may move resolution odds before Kalshi mid updates. A frozen clip bounds that pull so a one-minute spike cannot assert near-certainty. [H]

Not W2-A (official CF vs K). Not W2-E (side of FLOOR_STRIKE). Not F2 (remainder lock).

## CONTRACT / UNIVERSE SCOPE

- DATA-PROV-PM-003, TEST-007 excludes, rem=840 only
- KALSHI incumbent. Coinbase = instrument tape only
- No BTC+ETH pool. No T-10m/T-5m shop

## DECISION-TIME / KNOWABILITY

- \(m_t\), rem=840: PM-003 same row [V]
- \(v_{\mathrm{CB},t}\): CB-001 bars with `bar_end < t` [V lock B; Clock CONDITIONAL]
- FORBIDDEN: `bar_end == t` (policy A); Binance fill; CF print; Poly last; T−1 mid; EXPIRATION_VALUE; Map 2

## DATA LAYER

- L1: DATA-PROV-PM-003, DATA-PROV-PM-001 (CLOSE_TIME for gate)
- L3 exchange: **DATA-PROV-CB-001** only (not oracle, not L3-001 Binance)
- Fields: Kalshi mid; CB `close`, `bar_end`

## TRANSFORMS / NORMALIZATION

- Missing bar/prior → ABSTAIN, no fill
- clip \(v/w\) to ±1; clip \(p_t\) to \((\varepsilon,1-\varepsilon)\)
- No learned weights

## EXPECTED SIGN / USE IN FORECAST

Positive Coinbase 1m return raises \(p_t\) vs mid (capped); negative lowers it. Not an ensemble. Not a trade.

## FAILURE REGIME

Mid already prices the last Coinbase minute; 1m noise; Clock on-minute caveats if someone uses A; close-minute speech (gate should block).

## FALSIFICATION

On **each** headline separately vs same-t Kalshi mid:

Skill requires **both** ΔBrier < 0 and ΔLogLoss < 0 (model − market).  
Kill / REDUNDANT if ΔBrier ≥ 0 **or** ΔLogLoss ≥ 0, or CIs cover 0.  
Also kill if one headline works and the other inverts (no pool).

Placebos:
1. Shuffle \(v\) vs \(m_t\) within cell
2. λ = 0 (must give Δ = 0)
3. Flip sign of \(v\)
4. Policy A join (`bar_end <= t`) — must not be scored as this card

Do not retune λ/\(w\), lock B, or headlines after the sheet.

## LEAKAGE & REDUNDANCY CONTROLS

- Ablation vs \(m_t\) alone required
- Not W2-A / W2-E / W2-B / F2
- USED_RESEARCH if scored on PM-003

## Evidence notes

Thesis [H]. λ/\(w\)/ε [A] frozen before sheet. Headlines + lock B [V]. Measurement **UNTESTED**. No invented Δ. Trade FORBIDDEN.

## Examiner result — TEST-20260913-005 (2026-09-13T20:59:30Z)
- **VERDICT:** REDUNDANT / FAIL-INSUFFICIENT (not NO_EDGE; not Champion)
- Both T-14m headlines ΔBrier>0 and ΔLogLoss>0 vs mid:
  - BTC: ΔBrier=+0.002485 ΔLogLoss=+0.005974 N=604 speak=604
  - ETH: ΔBrier=+0.002852 ΔLogLoss=+0.006566 N=604 speak=604
- λ=0.15 w=0.0015 frozen; robustness grids fail — do not pick winner
- Join **CONDITIONAL**; Policy B (bar_end < t); no CF; no Poly last; no policy A
- Gate: DRAFT-ABST-20260913-005
- Artifacts: `archive/tests/TEST-20260913-005-CBVEL-INCREMENTAL.{md,json}`
- **No cemetery**
- Wave 007 CLOSED on this kill
- Trade FORBIDDEN; invented_numbers=false
