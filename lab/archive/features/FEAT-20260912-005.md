# FEAT-20260912-005 — F2 locked settlement-window remainder
**STATUS:** INACTIVE · tested TEST-20260913-001 **REDUNDANT / FAIL-INSUFFICIENT**
**FAMILY:** F2  
**WAVE:** post-001 (Governor attachment 2026-09-12 — scientific kernel only)
**FEATURE_ID:** FEAT-20260912-005 (Conductor stamp)

## What this is
Kalshi 15m YES iff (simple 60s CF RTI average **before CLOSE**) ≥ (simple 60s CF RTI average **before OPEN** = FLOOR_STRIKE).

If we had 1s CF ticks in the close-minute, after k locked seconds:
  locked_mean = mean(ticks[0:k])
  required_remaining_avg = (K * 60 - sum(ticks[0:k])) / (60-k)
Feature: distance of live CF print (or remaining required avg) vs K, mapped to P(YES) under a pre-registered remainder model.
**Not** Binance-as-oracle. **Not** a trimmed-mean (Kalshi rules on this series: **simple** 60s average `[V]` PM-001). **Not** a last-10s auto-trader.

## Why it might beat mid [H]
Once enough close-minute ticks lock, P(YES) can collapse while a sticky mid still prices a live spike. That is F2: official index path vs m_t.

## Why it may be dead on this venue [I]
Clock: Kalshi T−0 mid is near 0/1 on 1206/1208 PM-003 rows. T−1m already high near-deg. The fictional “last 10 seconds the book is still 40¢” is **not** what we observed.

## DATA
DATA-PROV-CF-001 **CLEARED** (2026-09-13). Entitled Kalshi CF passthrough. 1208/1208 close minutes 60/60 1Hz; pre_close_last 1208/1208. Missing-sec rate 0 on close minutes. Not Binance. Keys not in tree.

## Forbidden
Unauthorized CF websocket. Bypass of license. Binance substitute. Trading. Retune of F1/F3. Holdout open.

## Examiner
**TEST-20260913-001** F2-INCREMENTAL vs T−1m mid. USED_RESEARCH. Overall **REDUNDANT / FAIL-INSUFFICIENT** (ETH k=30 ΔLogLoss > 0). Not NO_EDGE. No cemetery. No retune. No holdout. No trade.

| Cell | N | ΔBrier | ΔLogLoss | Verdict |
|------|--:|-------:|---------:|---------|
| BTC CLOSE-k30 | 219 | −0.108110 | −0.085749 | INCREMENTAL_RESEARCH |
| ETH CLOSE-k30 | 178 | −0.048276 | +0.323900 | REDUNDANT / FAIL-INSUFFICIENT |

Annex (not headline; no post-hoc switch): k=10 both REDUNDANT; k=50 both INCREMENTAL_RESEARCH (ETH Brier 0 on 178). Hard 0/1 Map 2 is the ETH logloss bruise, not a data bug.
Artifacts: `harness/examiner/out/TEST-20260913-001-F2-INCREMENTAL.{md,json}` · `archive/tests/TEST-20260913-001-F2-INCREMENTAL.{md,json}`

## FROZEN MAP (2026-09-12 Conductor) [V]
Implementation: `harness/f2/evaluate_close_window.py`  
Tag: `FEAT-20260912-005/evaluate_close_window/2026-09-12-patch2`

- Map 1: unit-test predicate only. `lock_yes` / `lock_no` only at k=60.
- Map 2 (scored map, when data exists): last **locked** slot projects leftover seconds; `p_remainder` = 1 iff `projected_sum >= K*60`, else 0. k=0 uses `pre_close_last` or MISSING.
- Clock: `close_start_ms % 1000 == 0` else BAD_WINDOW. Slot i locked iff `(s_start+i+1)*1000 <= now_ms`. One print per second = `max(timestamp_ms)` in that second. Missing locked slot → MISSING (do not shrink /60).
- Eight synthetic fixtures pass. Real incrementality vs `MKT-KALSHI-15M-MID` was tested TEST-20260913-001: headline fail (ETH logloss). Function remains closed. No v2 map.
- No v2/v4 class, no entropy/k_lock, no BUY, no Binance oracle. Function is closed. Next ≠ retune k or drop ETH after seeing the sheet.
