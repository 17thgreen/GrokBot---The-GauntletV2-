# FEAT-20260912-001 — F1-A Structural digital (frozen σ)

- **FEATURE_ID:** FEAT-20260912-001
- **NAME:** Structural P(YES) from moneyness × τ with frozen σ
- **STATUS:** INACTIVE
- **FAMILY:** F1
- **WAVE:** FEATURE_WAVE_001_2026-09-12
- **PROVENANCE:** The Statistician · 2026-09-12 UTC · Wave 001 F1 · informal F1-A · incumbent MKT-KALSHI-15M-MID / TEST-20260911-007 · DATA-PROV-PM-003 + DATA-PROV-PM-001 · L3 price-at-t DATA-* TBD (Conductor) · OHLCV CEM-20260910-001/002/003 cited as different domain, not reused
- **CONFIDENCE:** Low [H] — originator prior; measured REDUNDANT/FAIL-INSUFFICIENT on TEST-20260912-002
- **PRETEST_QUERY:** QUERY-20260912-001
- **PRETEST_VERDICT:** CLEAR_TO_HYPOTHESIS
- **TEST:** TEST-20260912-002 — REDUNDANT / FAIL-INSUFFICIENT

## CARD LOCK (2026-09-12 Statistician addendum) [V]
- Scored blend is the single pre-registered λ=0.35 only. Do not fit λ on PM-003.
- Grid {0.20, 0.35, 0.50} = robustness annex; never select by Δ on this slice.
- Headline cells only: `KALSHI|15m|{BTC,ETH}|T-5m|mid`. Other four cells annex-only if measured — no retune after peek.
- No Examiner until L3 Clock-clear.


## DEFINITION
Kalshi 15m YES iff end-60s CF RTI average ≥ start-60s CF RTI average. Let K = FLOOR_STRIKE (start ref). Let τ = time_remaining_sec / (365.25·24·3600) [A calendar]. Let S_t = last fully completed L3 1m close with bar_end ≤ t (L3 proxy — NOT BRTI, NOT EXPIRATION_VALUE).

Zero-drift GBM digital [A]:
  z_t = ln(S_t / K) / (σ √τ)
  p_struct = Φ(z_t)
  p_t = clip( (1-λ) m_t + λ p_struct, ε, 1-ε )

Φ = standard normal CDF. ε = 1e-4 [A]. If S_t missing (no L3 bar within 90s of t) or τ≤0 or K≤0 → feature missing; do not score that row as a filled p_struct (Examiner: drop or mark missing — declare).

### PRE-REGISTERED PRIMARY CELL (no post-hoc switch)
Headline cells (separate, no pool): `KALSHI|15m|BTC|T-5m|mid` and `KALSHI|15m|ETH|T-5m|mid`.
Primary params: λ=0.35; σ_BTC=0.55; σ_ETH=0.70; ε=1e-4. Units: σ annualized log-vol [A].
Robustness (not headline): all six frozen cells; λ∈{0.20,0.35,0.50}; σ_BTC∈{0.40,0.55,0.70}; σ_ETH∈{0.55,0.70,0.85}. λ=1 (pure structural) and σ→0 step 1{S_t≥K} are diagnostics only.

## MECHANISM
Binary Up/Down is a digital on the remaining path vs a known start-ref K. Distance ln(S_t/K) scaled by remaining vol σ√τ is the structural P(S_T≥K) under GBM. Mid m_t already prices this imperfectly; incremental claim is that a honest S_t adds refinement vs sticky/noisy mid. [H]
60s TWAP settlement vs 1m last S_t is approximation error [A] — not an oracle claim.

## CONTRACT / UNIVERSE SCOPE
- Universe: DATA-PROV-PM-003 1208 (Kalshi 15m BTC/ETH remaining vs PM-002). Same exclude as TEST-007: mid-only, drop last-fallback, drop implied_p≤0.02 or ≥0.98, VOID/DISPUTED out.
- VENUE_ADAPTER: KALSHI only. No Poly. No BTC+ETH pool. No checkpoint pool.

## DECISION-TIME / KNOWABILITY
- K = FLOOR_STRIKE from DATA-PROV-PM-001 for same CONTRACT_ID. Value = pre-OPEN 60s CF average — treated as knowable after OPEN [A]. Clock must confirm no post-settlement revision of that field vs live start-ref.
- τ = PM-003 time_remaining_sec at checkpoint (840/600/300) — knowable [V] by construction.
- m_t = PM-003 implied_p mid — knowable [V] on CLEARED cells.
- S_t = L3 1m close ≤ t — NOT knowable until Conductor DATA-* + Clock. DATA-PROV-001 ends 2026-08-31 and does NOT cover PM-003 window [V].
- FORBIDDEN at t: EXPIRATION_VALUE, PM-001 LAST_PRICE / OUTCOME_PRICES, terminal last, independent CF BRTI treated as settlement [V order].
- Latency: S_t usable only after bar_end; no unfinished 1m bar.
- Clock: L3 join timestamps + FLOOR_STRIKE revision policy required before Examiner.

## DATA LAYER
- L1 PM: DATA-PROV-PM-003 (m_t, τ, contract_id, decision_time); DATA-PROV-PM-001 (FLOOR_STRIKE, RESOLUTION, OPEN/CLOSE).
- L2 oracle: CF BRTI/ETHUSDRTI replay UNTESTED — do not use; F2 owns oracle divergence.
- L3 external: **REQUIRED** — new DATA-* 1m BTC/ETH covering PM-003 opens 2026-09-04→11. Layer-3 only. If Binance Vision: L3 proxy [A], **not** settlement oracle [V].
- Fields: S_t close, bar_end; K; τ; m_t; y = RESOLUTION.

## TRANSFORMS / NORMALIZATION
- No silent row drops except declared missing S_t / exclude rules.
- clip p to [ε,1-ε] for logloss only; report unclipped p_struct in annex.
- No winsor of S_t/K unless |ln(S/K)|>0.05 → still compute Φ (fat tail ok); do not cap z.
- Mid construction unchanged from TEST-007 (no last-fallback in scored set).

## EXPECTED SIGN / USE IN FORECAST
- p_struct ↑ as S_t/K ↑ and as τ ↓ when S_t>K (and conversely).
- Enters as convex blend with m_t; λ locked pre-results at **0.35** (see CARD LOCK).
- Not an ensemble-of-ensembles. Not a trade.

## FAILURE REGIME
L3 ≠ CF TWAP (basis / weekend / venue); mid already efficient; σ mis-scale; T-14m vol-of-vol; missing S_t cluster; FLOOR_STRIKE revision.

## FALSIFICATION
On each headline cell, incremental model vs incumbent mid (same N, same t):
Kill / REDUNDANT if ΔBrier ≥ 0 and ΔLogLoss ≥ 0 (model − market; lower-is-better) or CIs cover 0 after pre-registered method.
Also kill if only one asset “works” and the other inverts (no pooled rescue).
Placebos: shuffle S_t vs (K,τ) within cell; λ=0 (must give Δ=0).
Do not score a model that uses EXPIRATION_VALUE.

## LEAKAGE & REDUNDANCY CONTROLS
- Ablation vs m_t alone (λ=0) required.
- Ablation vs trivial metadata (asset, τ only, no S_t) required as cousin — if τ-only matches F1-A, feature is not oracle-distance.
- Cross-layer: not F2 (no CF replay). Not F3 (no bid/ask microstructure).
- Slice already USED_RESEARCH — results = RESEARCH, not validation, not sealed.

## Evidence notes
Thesis [H]. GBM/μ=0/σ/λ/ε/τ-calendar [A]. Mid+τ knowable [V] on PM-003 CLEARED cells. S_t DATA [U] until new DATA-*. Independent CF replay [U]. Measurement: TEST-20260912-002 REDUNDANT/FAIL-INSUFFICIENT. Trade FORBIDDEN.

## Archivist filing note (2026-09-12)
- Canonical ID lock [V]: F1 = FEAT-20260912-001/002; F3 = 003/004 (collision resolved; prior swap void).
- STATUS HYPOTHESIS. No TEST. L3 DATA gate open.
- Cite CEM-20260910-001/002/003 as different-domain only.

## Statistician CONFIRM+PATCH (2026-09-12T00:13:05Z)
λ/CELL LOCK restamped. Canonical ID = FEAT-20260912-001 (F1-A). No TEST.

## Examiner result — TEST-20260912-002 (2026-09-12T00:20:53Z)
- **VERDICT:** REDUNDANT / FAIL-INSUFFICIENT (not NO_EDGE)
- Headlines λ=0.35: BTC T-5m ΔBrier=+0.003343 ΔLogLoss=+0.013816 (N=569); ETH T-5m ΔBrier=+0.004107 ΔLogLoss=+0.018475 (N=545) — both ≥0 vs mid
- All annex cells fail; λ grid {0.20,0.35,0.50} fail (do not retune)
- Artifacts: `archive/tests/TEST-20260912-002-F1-INCREMENTAL.{md,json}`
- **No cemetery** — REDUNDANT/FAIL-INSUFFICIENT ≠ NO_EDGE
- L3 via DATA-PROV-L3-001; Binance not oracle; no EXPIRATION_VALUE; trade FORBIDDEN; invented_numbers=false
